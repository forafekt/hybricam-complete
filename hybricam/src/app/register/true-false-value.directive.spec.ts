import { TrueFalseValueDirective } from './true-false-value.directive';

describe('TrueFalseValueDirective', () => {
  it('should create an instance', () => {
    const directive = new TrueFalseValueDirective();
    expect(directive).toBeTruthy();
  });
});
